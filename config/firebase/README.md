# Firebase configuration hub

The game is fully playable offline. Later, register Android and iOS apps using `com.dysonc.cricket`, run `dart pub global activate flutterfire_cli`, then run `flutterfire configure`. Never commit service-account files, signing keys, private tokens, or Apple certificates.
